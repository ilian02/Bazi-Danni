
  /*z1.1
  SELECT MOVIEEXEC.NAME
  FROM MOVIEEXEC
  WHERE MOVIEEXEC.NETWORTH > 10000000
  AND MOVIEEXEC.NAME IN (SELECT MOVIESTAR.NAME
						FROM MOVIESTAR
						WHERE MOVIESTAR.GENDER = 'F')
*/

/*z1.2
SELECT MOVIESTAR.NAME
FROM MOVIESTAR
WHERE MOVIESTAR.NAME NOT IN (SELECT MOVIEEXEC.NAME
							FROM MOVIEEXEC)
*/

/*z1.3
SELECT MOVIE.TITLE
FROM MOVIE
WHERE MOVIE.LENGTH > (SELECT MOVIE.LENGTH
					FROM MOVIE
					WHERE MOVIE.TITLE = 'Star Wars')
*/

/*z1.4
SELECT MOVIE.TITLE, MOVIEEXEC.NAME
FROM MOVIE, MOVIEEXEC
WHERE MOVIEEXEC.CERT# = MOVIE.PRODUCERC# AND
MOVIEEXEC.NETWORTH > (SELECT MOVIEEXEC.NETWORTH
					FROM MOVIEEXEC
					WHERE MOVIEEXEC.NAME = 'Merv Griffin')
*/


/* z2.1
SELECT product.maker
FROM product
WHERE product.model IN (SELECT pc.model
						FROM PC
						WHERE PC.speed > 500)
						*/

/*z2.2
SELECT printer.code, printer.model, printer.price
FROM printer
WHERE printer.price = (SELECT TOP 1 printer.price
					FROM printer
					ORDER BY printer.price DESC)
*/

/*z2.3
SELECT *
FROM laptop
WHERE laptop.speed < (SELECT TOP 1 pc.speed
					FROM pc
					ORDER BY pc.speed ASC)
*/

/* 2.4
SELECT TOP 1 prod.model, prod.price
FROM 
(SELECT pc.model, pc.price
FROM pc
UNION
SELECT laptop.model, laptop.price
FROM laptop
UNION
SELECT printer.model, printer.price
FROM printer)
as prod
ORDER BY prod.price DESC
*/

/*z2.5
SELECT TOP 1 product.maker
FROM product, printer
WHERE product.model = printer.model AND printer.price = 
(SELECT TOP 1 printer.price
FROM printer
WHERE printer.color = 'y'
ORDER BY printer.price ASC)
*/

/*z2.6
SELECT DISTINCT product.maker
FROM product
WHERE product.maker IN (
SELECT product.maker
FROM product, pc
WHERE product.model = pc.model 
AND pc.ram = 
	(SELECT TOP 1 pc.ram
	FROM pc
	ORDER BY pc.ram ASC)
)
*/

/*z3.1
SELECT DISTINCT CLASSES.COUNTRY
FROM CLASSES
WHERE CLASSES.NUMGUNS = (SELECT TOP 1 CLASSES.NUMGUNS
						FROM CLASSES
						ORDER BY CLASSES.NUMGUNS DESC)
*/

/*z3.2
SELECT DISTINCT SHIPS.CLASS
FROM SHIPS
WHERE SHIPS.NAME IN (SELECT OUTCOMES.SHIP
FROM OUTCOMES
WHERE OUTCOMES.RESULT = 'sunk')
*/

/*z3.3
SELECT SHIPS.NAME, CLASSES.class
FROM SHIPS, CLASSES
WHERE SHIPS.CLASS = CLASSES.CLASS AND CLASSES.BORE = 16
*/

/*z3.4
SELECT BATTLES.NAME
FROM BATTLES
WHERE BATTLES.NAME IN 
(SELECT OUTCOMES.BATTLE
FROM OUTCOMES, SHIPS
WHERE OUTCOMES.SHIP = SHIPS.NAME AND SHIPS.CLASS = 'Kongo')
*/

/*z3.5
SELECT SHIPS.CLASS, SHIPS.NAME
FROM SHIPS, CLASSES C
WHERE SHIPS.CLASS = C.CLASS AND C.NUMGUNS >= (
SELECT TOP 1 CLASSES.NUMGUNS
FROM CLASSES
WHERE CLASSES.BORE = C.BORE
ORDER BY CLASSES.NUMGUNS DESC)
*/